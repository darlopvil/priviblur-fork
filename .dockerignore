# Entornos virtuales
.venv/
venv/

# Cache de Python
__pycache__/
**/__pycache__/
*.pyc
*.pyo
.ruff_cache/

# Despliegue y configuracion local
deploy/
docker-compose*.yml
config.toml
*.log

# Cosas de repositorio que no pintan nada en la imagen
.github/
screenshots/
instances.md
requirements-dev.txt

# El repositorio git ya no hace falta dentro de la imagen: el hash del commit
# se inyecta con --build-arg PRIVIBLUR_COMMIT (ver docker/Dockerfile).
.git/
.gitignore